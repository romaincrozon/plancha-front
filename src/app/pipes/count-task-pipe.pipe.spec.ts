import { CountTaskPipePipe } from './count-task-pipe.pipe';

describe('CountTaskPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CountTaskPipePipe();
    expect(pipe).toBeTruthy();
  });
});

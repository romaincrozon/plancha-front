import { DirectiveAddresourceDirective } from './directive-addresource.directive';

describe('DirectiveAddresourceDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveAddresourceDirective();
    expect(directive).toBeTruthy();
  });
});

import { ResourceCalendar } from './resource-calendar.model';

describe('ResourceCalendar', () => {
  it('should create an instance', () => {
    expect(new ResourceCalendar()).toBeTruthy();
  });
});
